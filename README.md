# 对话网站 - Chat Website

一个现代化的对话网站，支持实时聊天、主题切换、文件附件等功能。

## 功能特性

- 🎨 **现代化界面设计** - 使用渐变背景和圆角设计
- 💬 **实时消息发送** - 支持Enter键快速发送
- 🤖 **自动回复功能** - 模拟对话伙伴的智能回复
- ⌨️ **打字指示器** - 显示对方正在输入的动画效果
- 😀 **表情符号支持** - 点击表情按钮插入随机emoji
- 📎 **文件附件功能** - 支持选择文件并显示文件信息
- ⚙️ **设置面板** - 可自定义用户名、主题和通知设置
- 🌓 **深色/浅色主题** - 支持主题切换
- 📱 **响应式设计** - 适配移动设备和桌面设备
- 🔒 **安全优化** - 配置了CSP和安全头
- ⚡ **离线支持** - Service Worker缓存支持

## 技术栈

- **HTML5** - 语义化标记和现代结构
- **CSS3** - 使用CSS变量、动画和媒体查询
- **JavaScript ES6+** - 面向对象编程和现代语法
- **Service Worker** - 离线缓存支持

## Cloudflare Pages 部署

### 方法一：通过 Git 仓库部署

1. **上传代码到 Git 仓库**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-repo-url>
   git push -u origin main
   ```

2. **在 Cloudflare Pages 中创建项目**
   - 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
   - 进入 "Pages" 部分
   - 点击 "Create a project"
   - 连接你的 Git 仓库
   - 选择构建设置（无需构建命令，直接部署）

3. **部署配置**
   - 构建命令：留空
   - 构建输出目录：`/`
   - 环境变量：无需设置

### 方法二：通过 Wrangler CLI 部署

1. **安装 Wrangler CLI**
   ```bash
   npm install -g wrangler
   ```

2. **登录 Cloudflare**
   ```bash
   wrangler login
   ```

3. **部署到 Workers**
   ```bash
   wrangler pages deploy .
   ```

### 方法三：直接上传部署

1. **打包项目文件**
   ```bash
   zip -r chat-website.zip . -x "*.git*" "node_modules/*" "README.md"
   ```

2. **在 Cloudflare Pages 中直接上传**
   - 登录 Cloudflare Dashboard
   - 进入 Pages
   - 点击 "Create a project"
   - 选择 "Upload assets"
   - 上传打包的 zip 文件

## 自定义域名

1. 在 Cloudflare Pages 项目设置中
2. 进入 "Custom domains" 部分
3. 添加你的自定义域名
4. 配置 DNS 记录

## 环境变量配置

项目支持以下环境变量（可选）：

- `ENVIRONMENT` - 运行环境（production/staging）

## 性能优化

已配置的优化：

- **缓存策略** - 静态资源长期缓存
- **压缩** - Cloudflare 自动压缩
- **CDN** - 全球 CDN 分发
- **安全头** - CSP、XSS 保护等
- **离线支持** - Service Worker 缓存

## 本地开发

### 使用 Node.js 服务器

```bash
node server.js
```

访问 `http://localhost:8000`

### 使用 Python 服务器

```bash
python -m http.server 8000
```

访问 `http://localhost:8000`

## 文件结构

```
chat-website/
├── index.html          # 主页面
├── styles.css          # 样式文件
├── script.js           # 功能脚本
├── sw.js              # Service Worker
├── server.js          # 本地开发服务器
├── _redirects         # Cloudflare Pages 重定向规则
├── _headers           # Cloudflare Pages 安全头
├── wrangler.toml      # Wrangler 配置文件
└── README.md          # 项目说明
```

## 浏览器支持

- Chrome 60+
- Firefox 55+
- Safari 12+
- Edge 79+

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！
